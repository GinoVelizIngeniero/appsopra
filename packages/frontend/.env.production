# URL de la API en producción. Vite la inyecta en el build (`vite build`).
# Así el frontend no necesita configurar variables de entorno en Vercel.
VITE_API_URL=https://appsopra-backend-d81w-ten.vercel.app/api
